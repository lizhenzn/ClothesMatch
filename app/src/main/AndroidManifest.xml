<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a74104.clothesmatch">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HaoYou.ChatUIActivity"></activity>
        <activity android:name=".HaoYou.HaoYouActivity" />
        <activity android:name=".MyInfo.EditNiChenActivity" />
        <activity android:name=".ClothesMatch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Match" />
        <activity android:name=".Square" />
        <activity android:name=".Introduce" />
        <activity android:name=".MyInfo.YiGuiActivity" />
        <activity android:name=".MyInfoActivity" />
    </application>

</manifest>